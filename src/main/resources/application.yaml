spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tech_store_version_1?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # dev OK, prod nên dùng validate
    show-sql: true
    properties:
      hibernate:
        database-platform: org.hibernate.dialect.MySQLDialect
        format_sql: true

  jackson:
    time-zone: Asia/Ho_Chi_Minh
    date-format: yyyy-MM-dd HH:mm:ss

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${SPRING_MAIL_USERNAME:volttech.store01@gmail.com}
    password: ${SPRING_MAIL_PASSWORD:onbgcktlphfrssno}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

server:
  port: 8080

# JWT Config
jwt:
  secret: "your-super-secret-key-at-least-256-bits-long-here"
  access-token-expiry: 86400000
  refresh-token-expiry: 604800000

# Cloudinary
cloudinary:
  cloud-name: your_cloud_name
  api-key: your_api_key
  api-secret: your_api_secret

# CORS
cors:
  allowed-origins: "http://localhost:5173"

# ================= VNPAY CONFIG =================
vnpay:
  tmnCode: GPJW0KVQ
  hashSecret: NAV0ZNEPOC0DSAQ9T2WIAC840UF7NO4R
  payUrl: https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
  returnUrl: http://localhost:8080/api/payment/vnpay-return
  apiUrl: https://sandbox.vnpayment.vn/merchant_webapi/api/transaction
  version: 2.1.0
  command: pay
  orderType: other
  locale: vn
  currCode: VND

# ================= VIETQR CONFIG =================
vietqr:
  accountNumber: "0344998620"
  accountName: "PHAM VAN VU"
  bankCode: "MB"
  bankName: "MB BANK"
  template: compact
  qrCodeSize: 300

# ================= GEMINI AI CONFIG =================
gemini:
  api:
    key: ${GEMINI_API_KEY:}